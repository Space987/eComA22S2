<?php 
	// include(); // try to import a file and warn us on failure
	// include_once(); // try to import a file (if not previously imported) and warn is on failure
	// require(); // try to import a file and halt on failure
	// require_once(); // try to import a file (if not previously imported) and halt on failure
	require_once('app/core/init.php');

	new \app\core\App();